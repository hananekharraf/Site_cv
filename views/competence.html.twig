{% extends "layout.html.twig" %}

{% block title %}Home{% endblock %}

{% block content %}
  <h1>Compétenes</h1>
  {% for competence in competences %}
    <article>
        <h2><a class="articleTitle" href="{{ path('competencee', { 'id': competence.id }) }}">{{ experience.title }}</a></h2>
        <p>{{ competence.content }}</p>
    </article>
  {% endfor %}

{% endblock %}
